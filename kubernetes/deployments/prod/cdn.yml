apiVersion: apps/v1
kind: Deployment
metadata:
  name: cdn
  namespace: school-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cdn
  template:
    metadata:
      labels:
        app: cdn
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      volumes:
      - name: cdn-prod
        persistentVolumeClaim:
          claimName: cdn-prod
      containers:
      - name: cdn
        image: "ghcr.io/emenec-finance/school/cdn:3"
        ports:
        - containerPort: 4100
        envFrom:
          - secretRef:
              name: app-secret
        volumeMounts:
        - name: cdn-prod
          mountPath: /data
        resources:
          limits:
            cpu: "1000m"
            memory: "1536Mi"
          requests:
            cpu: "750m"
            memory: "1024Mi"
        livenessProbe:
          exec:
            command:
            - ls
            - /data
          initialDelaySeconds: 5
          periodSeconds: 5
