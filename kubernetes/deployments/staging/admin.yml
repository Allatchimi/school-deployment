apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin
  namespace: school-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin
  template:
    metadata:
      labels:
        app: admin
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: admin
          image: "ghcr.io/emenec-finance/school-admin/school-admin:31"
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: admin-env-secret
          resources:
            limits:
              cpu: "250m"
              memory: "512Mi"
            requests:
              cpu: "100m"
              memory: "256Mi"
